{% form_theme form 'bootstrap_4_layout.html.twig' %}

{{ form_start(form, {'attr': {class: 'js-save-filter-form', novalidate: 'novalidate'}}) }}
{{ form_row(form.name, {'attr': {autofocus: 'autofocus'}}) }}
<br>
{{ form_rest(form) }}
{{ form_end(form) }}
